{
  "name": "@llmtrap/persona-engine",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "node -e \"require('node:fs').rmSync('dist',{ recursive: true, force: true })\"",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "node -e \"console.log('No tests defined for @llmtrap/persona-engine')\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@llmtrap/shared": "workspace:*"
  }
}